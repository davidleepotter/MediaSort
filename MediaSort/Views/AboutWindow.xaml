<Window x:Class="MediaSort.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About MediaSort"
        Icon="pack://application:,,,/Assets/AppIcon.ico"
        Height="280" Width="420"
        Background="{DynamicResource WindowBackground}"
        Foreground="{DynamicResource PanelForeground}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="MediaSort" FontSize="28" FontWeight="Bold"
                   Foreground="{DynamicResource PanelForeground}"/>
        <TextBlock Grid.Row="1" x:Name="VersionText" FontSize="14" Margin="0,4,0,0"
                   Foreground="{DynamicResource MutedForeground}"/>
        <TextBlock Grid.Row="2" Text="Copyright © Delos Technologies" Margin="0,16,0,0"
                   Foreground="{DynamicResource PanelForeground}"/>
        <TextBlock Grid.Row="3" TextWrapping="Wrap" Margin="0,8,0,0"
                   Foreground="{DynamicResource MutedForeground}"
                   Text="A keyboard-friendly tool for sorting images and videos into destination folders."/>

        <Button Grid.Row="4" Content="Close" Style="{StaticResource ToolButton}"
                HorizontalAlignment="Right" Width="90" Click="Close_Click"/>
    </Grid>
</Window>
