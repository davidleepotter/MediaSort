<Window x:Class="MediaSort.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About MediaSort"
        Height="280" Width="420"
        Background="#1E1E1E"
        Foreground="#EEE"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="MediaSort" FontSize="28" FontWeight="Bold"/>
        <TextBlock Grid.Row="1" x:Name="VersionText" FontSize="14" Opacity="0.8" Margin="0,4,0,0"/>
        <TextBlock Grid.Row="2" Text="Copyright © David Potter" Margin="0,16,0,0"/>
        <TextBlock Grid.Row="3" TextWrapping="Wrap" Opacity="0.7" Margin="0,8,0,0"
                   Text="A keyboard-friendly tool for sorting images and videos into destination folders."/>

        <Button Grid.Row="4" Content="Close" Style="{StaticResource ToolButton}" HorizontalAlignment="Right" Width="90" Click="Close_Click"/>
    </Grid>
</Window>
