<Window x:Class="MediaSort.Views.ProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Working…"
        Icon="pack://application:,,,/Assets/AppIcon.ico"
        Height="170" Width="460"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBackground}"
        Foreground="{DynamicResource PanelForeground}"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="HeaderText" Text="Copying file…"
                   FontSize="14" FontWeight="SemiBold"/>
        <TextBlock Grid.Row="1" x:Name="DetailText" Text="" Margin="0,4,0,0"
                   TextTrimming="CharacterEllipsis" Opacity="0.8"/>
        <ProgressBar Grid.Row="2" x:Name="Bar" Height="14" Margin="0,12,0,0"
                     Minimum="0" Maximum="100" Value="0"/>
        <TextBlock Grid.Row="3" x:Name="StatusText" Text="" Margin="0,6,0,0" Opacity="0.7"/>

        <Button Grid.Row="4" Content="Cancel" Width="90" HorizontalAlignment="Right"
                Click="Cancel_Click" Margin="0,8,0,0"/>
    </Grid>
</Window>
